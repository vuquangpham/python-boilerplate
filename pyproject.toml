[project]
name = "python-project"
version = "0.1.0"
description = "Example project for Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.11.13",
]
